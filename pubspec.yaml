name: nexus
description: A highly scalable, clean, and data-oriented framework for Flutter, inspired by ECS and powered by BLoC and GetIt.
version: 2.0.0-dev.1
homepage: https://github.com/ShoghShahadat/Nexus
repository: https://github.com/ShoghShahadat/Nexus
issue_tracker: https://github.com/ShoghShahadat/Nexus/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^8.1.4
  get_it: ^7.7.0
  meta: ^1.11.0
  collection: ^1.18.0
  go_router: ^16.2.0
  # analyzer was moved to dev_dependencies
  shared_preferences: ^2.2.3
  web: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
  build_runner: ^2.4.9
  source_gen: ^1.5.0
  build: ^2.4.1
  # analyzer moved to its correct place
  analyzer: ^6.4.1
